# 🍊 JuicOrganic

## 🚀 Installation
Open terminal and type the following and press enter:

1. Open the terminal in PhpStorm.
1. To check out your branch run `git checkout [YOUR NAME]` (e.g. `git checkout edda`)
2. Run `npm install`. This will install your Node.JS dependencies
3. Run `npm run sass`. This turns on automatic compilation from SCSS to CSS, run 